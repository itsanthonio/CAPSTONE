[tool:pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = apps
addopts = -v --tb=short
markers =
    django_db: Marks tests as using the database
    slow: Marks tests as slow
